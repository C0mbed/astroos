// src/AstroOS.Scheduler/IScheduler.cs
namespace AstroOS.Scheduler;

/// <summary>
/// TODO: Adaptive scheduler interface — selects optimal targets and manages session plan.
/// </summary>
public interface IScheduler
{
    // TODO(forge): Define GetNextTarget, ScoreTarget, UpdateConditions, PlanSession
}
