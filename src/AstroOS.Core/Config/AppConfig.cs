// src/AstroOS.Core/Config/AppConfig.cs
namespace AstroOS.Core.Config;

/// <summary>
/// TODO: Application configuration model — loaded from config/default.yaml and config/local.yaml.
/// </summary>
public sealed class AppConfig
{
    // TODO(forge): Define config properties — observatory profile, UI preferences, hardware defaults
}
